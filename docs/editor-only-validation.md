# 仅编辑布局验证（2026-09-18）

## 行为

- 预览标题栏的关闭按钮切换到 `editor-only`，编辑器占满可用内容区；`localStorage.editorLayout` 记住选择。
- “更多 → 编辑布局”提供仅编辑和两种分栏方向，恢复分栏使用当前编辑内容。
- 仅编辑时“另存为”后显示“恢复预览”，点击恢复 `lastSplitLayout` 保存的分栏方向并隐藏按钮；跨重启保留方向，没有历史记录时默认预览在左。
- 沿用 `Ctrl/⌘ + E` 切换完整阅读预览与编辑，不重新创建编辑器状态。
- 关闭时取消防抖/空闲调度、使待执行的 Mermaid 任务失效、释放 ECharts 实例并清空预览。已经开始的单次 Mermaid 计算不能中断，但结果被丢弃，后续图表不会继续渲染。
- 仅编辑期间保存、恢复备份仍然工作；显式导出使用最新编辑内容按需渲染，不恢复实时预览。

## 验证

- 307 项前端测试、`go test ./...`、`go vet ./...`、源代码发布安全门禁通过。
- 无头 Edge + 模拟文件桥接：全宽、隐藏期间无预览 DOM 更新、布局持久化、Ctrl/Meta 快捷键、光标、撤销/重做、保存、导出最新正文、恢复分栏通过。另验证约 31 万字符文档的延迟预览路径：仅编辑输入后预览保持空白，完整预览读取最新文字。
- 既有文档工具与冲突恢复浏览器回归通过；没有读写真实用户文档。
- 完整 Windows NSIS + 自定义启动器构建通过；不执行本机安装/卸载。macOS 快捷键为浏览器平台模拟，非 macOS 实机认证。
- 临时验证材料：`D:\工作\CodexTemp\quillite-editor-only-20260918-ea07aded`。
- 恢复按钮追加验证：308 项前端测试、Go 测试/静态检查、浏览器双方向恢复及跨重启记忆验证通过。新增截图位于 `D:\工作\CodexTemp\quillite-restore-preview-20260918-01`。

## 本地测试包

该功能将随 2.7.5 发布。

- 路径：`D:\mycode\quillite-markdown\build\bin\quillite-markdown-2.7.5-windows-amd64.exe`
- 大小：12,769,191 字节
- 修改时间：2026-09-18 18:36:29
- SHA-256：`57924FE6755CE8C53169B01E207D152D4BE5A66DB7FFC624C35189C36B0F140A`

普通 NSIS 卸载入口已改为严格校验下的原位运行；正式发布验收仍以 GitHub 托管 runner 的生命周期回归为准。
