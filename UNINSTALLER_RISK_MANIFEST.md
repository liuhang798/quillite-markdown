# Windows 卸载器风险名单维护

生产名单位于 `uninstaller_risk_hashes_windows.go`，当前为空。测试使用的假二进制摘要仅在 `_test.go` 临时注入，不能复制进生产名单。

添加真实条目之前必须记录并审核：

1. 原始正式发布来源、版本、构建提交、安装包 SHA-256。
2. 在隔离虚拟机中取得的 **uninstall.exe 本身**完整 SHA-256 与大小，保存样本，不在开发机上运行风险卸载程序。
3. 对应卸载脚本的危险行为证据；已修复的重构建即使版本相同也不能列为风险。
4. 多个干净安装中样本是否一致。如果生成结果因安装路径或重构建而不同，仅列入分别经过验证的摘要，不使用模糊匹配。
5. 复核安全标记优先、未知文件保留、精确登记匹配以及失败时保留数据的测试。

运行时不得下载名单、从用户机器自动学习、根据缺失标记推断风险，或回退到仅按版本/文件名识别。未知文件也不能被“禁用登记”间接处理。无法验证时保留原物并停止应用内更新。

操作使用拒绝并发写入/删除共享的文件句柄；只允许单链接普通文件，拒绝重解析点和目录；只删除已检查的同一文件对象。禁用改名不覆盖任何已存在目标。仅移除已确认风险文件的卸载登记，不等于阻止用户手动运行该文件。

已确认哈希且无匹配卸载登记时，启动、检查更新与更新执行采用一致的“无需再次处理登记”结果；不得再删除或改名该文件。未知哈希即使没有登记也不得放行或处理。
